# ============================================================================
# .env.example - Environment variable template for Job Finder
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control.
# ============================================================================

# ---------------------------------------------------------------------------
# Demo / Test Mode
# Set to "true" to use sample jobs (no real HTTP scraping)
# Set to "false" for production scraping from Indeed / LinkedIn
# ---------------------------------------------------------------------------
DEMO_MODE=true

# ---------------------------------------------------------------------------
# Google Sheets
#
# 1. Create a Google Cloud project at https://console.cloud.google.com
# 2. Enable the Google Sheets API and Google Drive API
# 3. Create a Service Account and download the JSON key file
# 4. Place the key file at the path below (or update the path)
# 5. Share your target Google Sheet with the service account email
#    (found in the JSON file under "client_email")
# ---------------------------------------------------------------------------

# Absolute or relative path to your service account JSON key file
GOOGLE_CREDENTIALS_PATH=credentials.json

# The ID of the Google Spreadsheet to write to.
# Found in the URL: https://docs.google.com/spreadsheets/d/<SPREADSHEET_ID>/edit
# Leave blank to auto-create a new spreadsheet on first run.
SPREADSHEET_ID=

# ---------------------------------------------------------------------------
# Email Notifications (Gmail example)
#
# For Gmail you MUST use an App Password, not your account password.
# Steps:
#   1. Enable 2-Factor Authentication on your Google account
#   2. Go to https://myaccount.google.com/apppasswords
#   3. Create an App Password for "Mail" / "Other"
#   4. Paste the 16-character app password below
# ---------------------------------------------------------------------------

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Your Gmail address (sender)
SMTP_USER=your_email@gmail.com

# App password (NOT your account password)
SMTP_PASSWORD=xxxx_xxxx_xxxx_xxxx

# Comma-separated list of recipient email addresses
NOTIFY_TO=your_email@gmail.com,another@example.com

# ---------------------------------------------------------------------------
# Job Search Configuration (optional overrides)
# ---------------------------------------------------------------------------

# Override the match threshold percentage (default: 72)
# MATCH_THRESHOLD_PCT=72

# ---------------------------------------------------------------------------
# Twilio SMS (optional – requires twilio package)
# ---------------------------------------------------------------------------

# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_FROM_NUMBER=+1XXXXXXXXXX
# TWILIO_TO_NUMBER=+1XXXXXXXXXX
